version: "2"

run:
  timeout: 3m

linters:
  enable:
    - govet
    - revive

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/alebabai/go-kafka
